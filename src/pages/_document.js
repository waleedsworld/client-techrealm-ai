import { Html, Head, Main, NextScript } from "next/document";

export default function Document() {
  return (
    <Html lang="en">
      <Head>
        <meta
          name="description"
          content="TechRealm AI — a dashboard for spinning up AI project briefs and tracking them from prompt to delivery."
        />
        <meta name="theme-color" content="#6B1B1C" />
        <link rel="icon" href="/favicon.png" />
      </Head>
      <body>
        <Main />
        <NextScript />
      </body>
    </Html>
  );
}
